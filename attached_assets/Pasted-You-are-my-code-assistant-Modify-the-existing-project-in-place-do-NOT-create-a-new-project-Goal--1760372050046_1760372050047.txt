You are my code assistant. Modify the existing project in place (do NOT create a new project).
Goal: Make the Home page visually match my Base44 reference (light theme) instead of the current dark/gradient look.

Global theme shift (light UI)

Create or update a global stylesheet (prefer src/styles/global.css; if it doesn’t exist, create it and import it in the app root).

Add CSS variables and base rules:

:root{
  --font-sans: Inter, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
  --color-bg: #FFFFFF;
  --color-surface: #FFFFFF;
  --color-muted: #F8FAFC;        /* light section bg */
  --color-text: #0F172A;         /* slate-900 */
  --color-text-muted: #475569;   /* slate-600 */
  --color-border: #E5E7EB;       /* gray-200 */

  --color-primary: #2563EB;      /* blue-600 */
  --color-primary-600: #2563EB;
  --color-primary-700: #1D4ED8;

  --color-purple: #7C3AED;
  --color-green: #22C55E;
  --color-amber: #F59E0B;

  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;

  --space-1: 4px; --space-2: 8px; --space-3: 12px; --space-4: 16px;
  --space-5: 20px; --space-6: 24px; --space-7: 32px; --space-8: 40px; --space-9: 56px; --space-10: 80px;

  --shadow-sm: 0 1px 2px rgba(0,0,0,.06);
  --shadow-md: 0 6px 18px rgba(2,6,23,.06);
}

*{box-sizing:border-box}
html,body,#root{height:100%}
body{
  margin:0;
  font-family:var(--font-sans);
  color:var(--color-text);
  background:var(--color-bg);
}

/* layout helpers */
.container{
  max-width:1200px;
  margin:0 auto;
  padding:0 24px;
}

/* buttons */
.btn{
  display:inline-flex; align-items:center; justify-content:center; gap:8px;
  padding:12px 18px; border-radius:12px; font-weight:600; text-decoration:none; border:1px solid transparent; cursor:pointer;
}
.btn-primary{ background:var(--color-primary); color:#fff; }
.btn-primary:hover{ background:var(--color-primary-700); }
.btn-outline{ background:#fff; border-color:var(--color-border); color:var(--color-text); }
.btn-outline:hover{ border-color:#CBD5E1; background:#F8FAFC; }

/* cards */
.card{
  background:var(--color-surface);
  border:1px solid var(--color-border);
  border-radius:16px;
  box-shadow:var(--shadow-sm);
  padding:24px;
}

/* headings */
h1{ font-weight:800; font-size: clamp(32px, 5.5vw, 48px); line-height:1.1; letter-spacing:.2px; margin:0; }
h2{ font-weight:800; font-size: clamp(24px, 3.5vw, 32px); margin:0; }
.lead{ color:var(--color-text-muted); font-size:18px; line-height:1.7; }

/* navbar/footer */
.navbar{
  position:sticky; top:0; z-index:50;
  background:#fff; border-bottom:1px solid var(--color-border);
}
.navlinks a{ color:#0F172A; text-decoration:none; font-weight:600; opacity:.9; }
.nav-actions .chip{ border:1px solid var(--color-border); border-radius:999px; padding:8px 14px; font-weight:600; background:#fff; }
.footer{
  background:#0B1220; color:#D1D5DB; padding:56px 0 24px; margin-top:80px;
}
.footer a{ color:#E5E7EB; text-decoration:none; }
.footer .muted{ color:#9CA3AF; font-size:14px; }


Ensure the global CSS is imported once (e.g., in src/main.jsx or src/App.jsx):

import './styles/global.css';


Remove/disable any site-wide dark gradient backgrounds and dark button styles. Replace ad-hoc colors with the variables above.

Navbar (light with subtle divider)

Edit the Nav component (e.g., src/components/Nav.jsx or similar). Implement:

White background, bottom border var(--color-border).

Left: logo “ConnectPlay” (keep your current mark).

Center/right: links “Universities · Industry Professionals · About · Contact”.

Right: “Student Login” (text) and “Admin Login” as a pill button (outline style).

Spacing: container height ~64px; link gap 20px.

Patch (adjust selectors to your file names):

<header className="navbar">
  <div className="container" style={{display:'flex',alignItems:'center',justifyContent:'space-between',height:64}}>
    <div style={{display:'flex',alignItems:'center',gap:12}}>
      {/* keep existing logo image if you have it */}
      <strong>ConnectPlay</strong>
    </div>
    <nav className="navlinks" style={{display:'flex',gap:20}}>
      <a href="/universities">Universities</a>
      <a href="/professionals">Industry Professionals</a>
      <a href="/about">About</a>
      <a href="/contact">Contact</a>
    </nav>
    <div className="nav-actions" style={{display:'flex',gap:12}}>
      <a href="/student-login">Student Login</a>
      <a className="chip" href="/admin-login">Admin Login</a>
    </div>
  </div>
</header>

Hero (white, centered, with blue accent on two words)

Replace the dark gradient hero with a white section and centered content.

Title copy: “Connecting Universities with Industry Experts” — the words Industry Experts should be blue (var(--color-primary)).

Subhead: use your Base44 sentence.

Remove the dark buttons; use outline/primary styles sparingly (Base44 hero has no primary CTA buttons in the snapshot).

Patch (e.g., src/components/Hero.jsx):

<section style={{background:'#fff', padding:'56px 0'}}>
  <div className="container" style={{textAlign:'left', maxWidth:900}}>
    {/* optional tiny logo row can stay above heading if you have it */}
    <h1>
      Connecting Universities with <span style={{color:'var(--color-primary)'}}>Industry Experts</span>
    </h1>
    <p className="lead" style={{marginTop:16}}>
      The premier platform for academic-industry partnerships. Universities manage student access, book verified guest speakers, and track engagement outcomes.
    </p>
  </div>
</section>

Three feature cards row (below hero)

Replace the current three dark cards with light cards as in Base44.

Each card has: icon circle, title, short paragraph, and a CTA button.

Backgrounds remain white; use a soft tinted border or left accent by color.

Patch (e.g., src/components/HomeCards.jsx):

<section style={{background:'#fff', padding:'32px 0 48px'}}>
  <div className="container" style={{display:'grid', gap:24, gridTemplateColumns:'repeat(3, minmax(0,1fr))'}}>
    {/* Card 1 – light blue tone */}
    <div className="card" style={{borderColor:'var(--color-border)'}}>
      <div style={{width:44,height:44,borderRadius:12, background:'#EFF6FF', display:'grid',placeItems:'center', marginBottom:12}}>
        {/* your university icon */}
      </div>
      <h3 style={{margin:'4px 0 8px'}}>For Universities</h3>
      <p className="lead" style={{fontSize:15}}>Partner with us to provide your students with access to industry professionals, curated content, and career opportunities at scale.</p>
      <a className="btn btn-outline" style={{marginTop:16}} href="/universities">University Partnerships</a>
    </div>

    {/* Card 2 – purple tone */}
    <div className="card" style={{borderColor:'var(--color-border)'}}>
      <div style={{width:44,height:44,borderRadius:12, background:'#F5F3FF', display:'grid',placeItems:'center', marginBottom:12}}>
        {/* speaker icon */}
      </div>
      <h3 style={{margin:'4px 0 8px'}}>Industry Professionals</h3>
      <p className="lead" style={{fontSize:15}}>Join our network of vetted guest speakers and share your expertise with classes that match your experience.</p>
      <a className="btn btn-primary" style={{marginTop:16}} href="/professionals">Become a Speaker</a>
    </div>

    {/* Card 3 – amber tone with outline */}
    <div className="card" style={{borderColor:'#FDE68A'}}>
      <div style={{width:44,height:44,borderRadius:12, background:'#FFFBEB', display:'grid',placeItems:'center', marginBottom:12}}>
        {/* student icon */}
      </div>
      <h3 style={{margin:'4px 0 8px'}}>Student Access</h3>
      <p className="lead" style={{fontSize:15}}>Students gain access through their university, use access codes provided by your school to join, and connect with verified experts.</p>
      <a className="btn btn-outline" style={{marginTop:16}} href="/student-access">Enter Access Code →</a>
    </div>
  </div>
</section>

“How ConnectPlay Works” (4 horizontal steps)

White background; big section heading centered.

Four equal items with colored icons: blue, green, purple, amber.

One-line label + short caption.

Patch (e.g., src/components/HowItWorks.jsx):

<section style={{background:'#fff', padding:'56px 0'}}>
  <div className="container">
    <h2 style={{textAlign:'center'}}>How ConnectPlay Works</h2>
    <div style={{marginTop:28, display:'grid', gap:24, gridTemplateColumns:'repeat(4, minmax(0,1fr))'}}>
      {[
        {title:'1. University Partners', text:'Universities sign up and choose their partnership package.', bg:'#EFF6FF'},
        {title:'2. Access Codes', text:'Universities generate access codes for their students to create accounts.', bg:'#ECFDF5'},
        {title:'3. Book & Access', text:'Universities book speakers while students access content and events.', bg:'#EEF2FF'},
        {title:'4. Track Impact', text:'Universities track student engagement and program outcomes.', bg:'#FFFBEB'},
      ].map((item,i)=>(
        <div key={i} style={{textAlign:'center'}}>
          <div style={{width:48,height:48,borderRadius:12, margin:'0 auto 10px', background:item.bg}} />
          <div style={{fontWeight:700, marginBottom:6}}>{item.title}</div>
          <div className="lead" style={{fontSize:15}}>{item.text}</div>
        </div>
      ))}
    </div>
  </div>
</section>

“Complete Platform…” (3 features row)
<section style={{background:'#fff', padding:'24px 0 56px'}}>
  <div className="container">
    <h2 style={{textAlign:'center'}}>Complete Platform for Academic-Industry Connections</h2>
    <div style={{marginTop:28, display:'grid', gap:24, gridTemplateColumns:'repeat(3, minmax(0,1fr))'}}>
      {[
        {title:'Verified Speakers', text:'Curated network of industry professionals across sports, media, and entertainment.', bg:'#EEF2FF'},
        {title:'Easy Booking', text:'Streamlined booking process with confirmed availability and automated reminders.', bg:'#ECFDF5'},
        {title:'Measurable Impact', text:'Track attendance, engagement, and student outcomes with detailed reporting.', bg:'#F5F3FF'},
      ].map((f,i)=>(
        <div key={i} style={{textAlign:'center'}}>
          <div style={{width:48,height:48,borderRadius:12, margin:'0 auto 10px', background:f.bg}} />
          <div style={{fontWeight:700, marginBottom:6}}>{f.title}</div>
          <div className="lead" style={{fontSize:15}}>{f.text}</div>
        </div>
      ))}
    </div>
  </div>
</section>

“Trusted by Leading Institutions” (logo/text band)
<section style={{background:'#fff', padding:'16px 0 48px'}}>
  <div className="container" style={{textAlign:'center'}}>
    <h2>Trusted by Leading Institutions</h2>
    <div style={{display:'grid', gridTemplateColumns:'repeat(4, minmax(0,1fr))', gap:24, marginTop:24}}>
      {['Stanford','USC','UCLA','NYU'].map((name,i)=>(
        <div key={i} style={{fontWeight:700, color:'#374151'}}>
          {name}
          <div style={{fontWeight:500, color:'#9CA3AF', marginTop:6}}>{['University','Annenberg','Extension','Tisch'][i]}</div>
        </div>
      ))}
    </div>
  </div>
</section>

Footer (dark with 4 columns)

Create/update a Footer component that replaces the current dark homepage block with a site footer only.

<footer className="footer">
  <div className="container" style={{display:'grid', gridTemplateColumns:'repeat(4, minmax(0,1fr))', gap:24}}>
    <div>
      <div style={{fontWeight:800, color:'#fff'}}>ConnectPlay</div>
      <p className="muted" style={{marginTop:10}}>Connecting universities with industry professionals for enhanced student learning.</p>
    </div>
    <div>
      <div style={{fontWeight:700, color:'#E5E7EB', marginBottom:8}}>Platform</div>
      <div><a href="/universities">Universities</a></div>
      <div><a href="/professionals">Industry Professionals</a></div>
      <div><a href="/student-access">Student Access</a></div>
    </div>
    <div>
      <div style={{fontWeight:700, color:'#E5E7EB', marginBottom:8}}>Company</div>
      <div><a href="/about">About Us</a></div>
      <div><a href="/contact">Contact</a></div>
      <div><a href="/partner">Partner with Us</a></div>
    </div>
    <div>
      <div style={{fontWeight:700, color:'#E5E7EB', marginBottom:8}}>Support</div>
      <div><a href="/help">Help Center</a></div>
      <div><a href="/partnerships">Partnerships</a></div>
    </div>
  </div>
  <div className="container" style={{borderTop:'1px solid rgba(255,255,255,.08)', marginTop:24, paddingTop:16}}>
    <div className="muted">© 2024 ConnectPlay. All rights reserved.</div>
  </div>
</footer>

Wiring on the Home page

In your Home page file (e.g., src/pages/Home.jsx or src/App.jsx), render components in this order:

<>
  <Nav />
  <Hero />
  <HomeCards />
  <HowItWorks />
  <Features /> {/* the “Complete Platform…” section */}
  <LogosBand /> {/* the “Trusted by…” section */}
  <Footer />
</>

Clean up & responsiveness

Remove the old dark gradient hero and dark cards.

Ensure no horizontal scrolling on mobile; stack grids at < 768px:

@media (max-width: 1024px){
  .container{ padding:0 20px; }
}
@media (max-width: 768px){
  .grid-3, .grid-4 { grid-template-columns: 1fr !important; }
  .navlinks{ display:none; } /* optional: collapse links on mobile */
}